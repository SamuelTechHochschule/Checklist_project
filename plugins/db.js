/*

import { Client } from 'pg'

const client = new Client({
    user: 'postgres',
    host: 'localhost',
    database: 'Checklist_db',
    password: 'JodTb745#s+f',
    port: 5432,
})

client.connect()

export default client

*/